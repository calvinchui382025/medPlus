import { styled } from '@mui/system';

export const Root = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  width: '100%',
  padding: '0px',
  // backgroundColor: 'green',
})